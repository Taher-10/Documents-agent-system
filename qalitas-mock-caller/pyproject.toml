[project]
name = "qalitas-mock-caller"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
  "fastapi>=0.135.0",
  "httpx>=0.28.0",
  "uvicorn>=0.41.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
